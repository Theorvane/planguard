services:
  - type: web
    name: planguard-api
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    autoDeployTrigger: commit
    envVars:
      - key: PLANGUARD_BOOTSTRAP_MODE
        value: "true"
      - key: NODE_ENV
        value: production
