{
  "name": "@planguard/api",
  "private": true,
  "version": "0.0.0",
  "description": "PlanGuard webhook and analysis API (docs/PRODUCT_PLAN.md #8).",
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js",
    "start": "node dist/src/main.js"
  },
  "dependencies": {
    "@planguard/github-client": "*",
    "@planguard/risk-engine": "*",
    "@planguard/schemas": "*",
    "@planguard/terraform-parser": "*"
  },
  "devDependencies": {
    "@octokit/webhooks-methods": "^6.0.0",
    "typescript": "^5.9.3"
  }
}
